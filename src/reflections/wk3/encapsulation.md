# Encapsulation

## Why use encapsulation?

it prevents data cuppling by only allowing data to be stored in one place and only one thing accesing that data this makes it easier if you want to go bak and change that data later

---

## Issues with closures and the uderscore prefix

a problem with the underscore prefix is that many new developers arent aware of what it means and they end up changing data inprivate data fields

---

## How do we create private variables in a ES6 Class?

the way to create private variables in a es6 class is closures and one reason wed do this is because data gets update instantly ersus having an extra step or two needed to be added to update data
